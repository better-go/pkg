package conf

import (
	"testing"
)

func TestTomlInit(t *testing.T) {
	//err := TomlInit("")
	//t.Log(err)
	//
	//db := new(cfg.MySQLConn)
	//t.Log("map:", TomlMap())
	//_ = TomlScan(db, "mysql")
	//t.Log("db:", db)
	//t.Log(db.Addr, db.Idle)
}
